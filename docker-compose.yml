services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: zugzwang
      POSTGRES_USER: zugzwang
      POSTGRES_PASSWORD: zugzwang
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
